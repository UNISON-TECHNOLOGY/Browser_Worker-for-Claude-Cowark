---
description: 動画広告台本生成 — LP/商材/広告画像から、プラットフォーム別の動画広告台本と絵コンテを生成する。Use when ユーザーが「動画広告の台本を作って」「TikTok/Reels/YouTube広告の構成案」「このLPを動画にして」「ショート動画のフック案」等、動画広告の企画・台本を依頼したとき。Not for 静止画バナー、動画ファイル自体の生成（対象外と伝える）。
argument-hint: <LPのURL または 商材の説明> [出稿先: tiktok/youtube/meta（省略時は3種全部）]
---

動画広告の台本・絵コンテを作成してください。入力: $ARGUMENTS

## 手順

1. **素材の把握**: 入力が URL なら knowledge/styles/・knowledge/watch/ の既存データを優先再利用（なければ read_page で訴求・実績数値・CTA文言を抽出）。広告画像が添付されていればそのトーン・コピーも入力に加える
2. **references/video-ad/SKILL.md を Read** し、その規範に従う:
   - 最強の1訴求を選定（1動画1メッセージ）→ フック型を選びフック代案3本
   - 黄金構造（0-3秒フック→価値→実証→CTA）で絵コンテ表を作成
   - 指定された出稿先の型（尺・比率・テロップ量）に調整。指定がなければ TikTok系/YouTube/Meta の3バージョン
3. **文言の品質**: コピーは copywriting スキル（13字ルール）、実証は LP の実数値のみ（捏造禁止・ないものは `[要確認]`）、CTA は LP 主CTA と同一文言
4. **検収**: SKILL.md 末尾のチェックリストで自己検査。lessons.md（knowledge/feedback/）があれば反映。**最終稿は references/ad-compliance-jp/SKILL.md の法規チェック（景表法・薬機法等）を必ず通してから納品**
5. **出力**: `knowledge/drafts/adscript-<name>-<date>.md` に保存し、チャットにも絵コンテ表を表示。複数プラットフォーム版は差分（尺・フック・テロップ）を明示

## 注意

- 動画ファイルの生成・編集は対象外（台本・絵コンテ・フック設計まで）。求められたら制作ツールへの受け渡し形式（この絵コンテ表）がそのまま使えると案内
- 他社事例の映像表現を「参考」として言及するのは可、コピーの転用は不可
