---
description: /delve-config の日本語エイリアス。タスクPack（機能グループ）のON/OFF
argument-hint: [list | on <pack> | off <pack>]
---

プラグインの `commands/delve-config.md`（見つからなければ Glob `**/delve-config.md`）を Read し、その手順に従って設定を操作してください。引数: $ARGUMENTS
