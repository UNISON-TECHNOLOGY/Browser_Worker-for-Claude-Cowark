---
description: /delve-deep の日本語エイリアス。動的ループ+動的専門エージェントによる徹底実行（明示要求時のみ）
argument-hint: <対象タスク>
---

プラグインの `commands/delve-deep.md`（見つからなければ Glob `**/delve-deep.md`）を Read し、その手順に従って徹底モードで実行してください。引数: $ARGUMENTS
