---
description: 生成AIによる画像作成 — ユーザーのログイン済みブラウザで Gemini / ChatGPT の画像生成を操作し、生成画像を knowledge/assets/ に取り込む。Use when ユーザーが「画像を作って/生成して」「Geminiで画像出して」「バナーの背景を生成して」等、画像の新規生成を求めたとき。既存素材の探索は対象外（→素材探し）、HTML/図の作成も対象外（→design-artisan）。（英語名: /delve-imagegen。手順の正本は procedures/delve-imagegen.md）
argument-hint: <生成したい画像の内容と用途>
---

プラグインの `procedures/delve-imagegen.md`（見つからなければ Glob `**/delve-imagegen.md`）を Read し、その手順に従って実行してください。引数: $ARGUMENTS
